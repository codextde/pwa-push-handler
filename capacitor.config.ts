import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.codext.pwa-push-handler',
  appName: 'PWA Push Handler',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
